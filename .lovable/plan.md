Ajustar el logo en la barra superior a 80 px de altura y subir la versión a 1.0.20.

### Cambios técnicos
1. `desktop/src/App.tsx` — Línea 138: cambiar `height: 40` a `height: 80` en `TopBarWithLogo`.
2. `desktop/package.json` — Bumpear `"version"` de `"1.0.19"` a `"1.0.20"`.